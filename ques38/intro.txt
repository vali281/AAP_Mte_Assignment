q. Generate all permutations of a given array. Write its algorithm, program. Find its time and space complexities. Explain with suitable example.
a. time and space complexity is O(n!) and O(n) respectively
